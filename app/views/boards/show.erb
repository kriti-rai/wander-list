<h2><em><font color = "maroon"><%=@board.name%></font></em></h2>
<br>
<%if @board.trips.empty?%>
  <h4><font color="grey">Currently the board is empty.</font></h4>
<%else%>
  <% @board.trips.each do |trip| %>
    <h4><a href = <%="#{trip.url}"%> target = "_blank"><%= trip.name %></a></h4>
    <% end %>
<%end%>
<br>
<% if @user == @board.user %>
  <p><a href='/boards/new'>Create a board </a> | <a href='/boards/<%=@board.id%>/edit'>Edit board</a> | <a href='/boards'>Boards</a> | <a href='/'>Home</a></p>
<% else %>
  <p><a href='/boards'>Boards</a> | <a href='/'>Home</a></p>
<% end %>
